import { Individual } from "../individual";
import { MatchTypeStrategy } from "./matchTypeStrategy";

export class DistanceBasedStrategy implements MatchTypeStrategy {
  matching(i:Individual,other:Individual[]){
    
  }
}